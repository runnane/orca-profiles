node_modules
dist
public/sample-config.json
e2e/screenshots
test-results
playwright-report
*.tsbuildinfo

# Agent tooling is committed (see AGENTS.md), except:
#  - local settings, which are per-machine
#  - .mcp.json, which names internal infrastructure and must not be published
.claude/settings.local.json
.mcp.json
dist-cli
dist-server
deploy.env

# Generated by scripts/make-fixture.mjs. Never commit a real config here.
fixtures/
